import React, { Component } from 'react';

export default class Header extends Component{
    render(){
        return(
            <h1 className = "communityName">Lick yarn when guests come over</h1>
        )
    }
}